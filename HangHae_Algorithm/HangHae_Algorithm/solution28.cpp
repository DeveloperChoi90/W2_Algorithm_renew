//ÇÏ»þµå ¼ö
#include<iostream>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	return 0;
}

/*
#include <string>
#include <vector>

using namespace std;

bool solution(int x) {
    bool answer = true;
    string s = to_string(x);
    int size = s.length();

    int tmp = 0;
    for(int i = 0 ; i < size; i++)
        tmp += s[i] - 48;

    if(x % tmp == 0)
        return answer;
    else
        return false;
}
*/