//콜라츠 추측
#include<iostream>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	

	return 0;
}

/*
#include <string>
#include <vector>

using namespace std;

int solution(int num) {
    int answer = 0;
    long long tmp = num;

    while(1)
    {
        if(answer >= 500)
        {
            answer = -1;
            break;
        }

        if(tmp == 1)
            break;
        else if(tmp % 2 == 0)
        {
            tmp = tmp / 2;
            answer++;
        }
        else if(tmp % 2 != 0)
        {
            tmp = tmp * 3 + 1;
            answer++;
        }
    }
    return answer;
}
*/